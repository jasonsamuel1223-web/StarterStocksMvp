# Frontend Environment Variables
# Copy this file to .env and fill in your values

# Backend API URL — leave empty to use Vite's dev proxy (recommended for local dev).
# Set this only if your backend runs on a different origin (e.g. for production).
VITE_API_BASE_URL=
